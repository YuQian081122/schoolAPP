FROM node:24-alpine

LABEL "language"="nodejs"
LABEL "framework"="express"

WORKDIR /app

# 複製 package.json
COPY package.json package-lock.json* ./

# 安裝依賴
RUN npm install

# 複製所有文件
COPY . .

# 暴露端口
EXPOSE 8080

# 啟動服務器
CMD ["npm", "start"]
