# This file contains the credentials for the voice & chat platforms
# which your bot is using.
# https://rasa.com/docs/rasa/user-guide/connectors/

rest:
  # No authentication. This should only be used for local development.
  # In production, you should use proper authentication.
  # See https://rasa.com/docs/rasa/user-guide/connectors/rest-api/#authentication
  # You can also use other authentication methods, see
  # https://rasa.com/docs/rasa/user-guide/connectors/rest-api/#authentication
  # for more details.
  
  # 注意：rest.message.received 錯誤通常是因為：
  # 1. 模型未訓練或訓練不完整
  # 2. 消息格式不正確
  # 3. Rasa 服務器配置問題
  # 
  # 解決方法：
  # 1. 執行 rasa/訓練模型.bat 重新訓練模型
  # 2. 檢查 Rasa 服務器視窗的錯誤信息
  # 3. 確保 Rasa 服務器和 Actions 服務器都在運行

